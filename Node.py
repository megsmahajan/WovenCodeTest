# Create a class Node to store its row and column value and its distance from the destination node.
class Node:

   def __init__(self, x, y, dist):
      self.x = x
      self.y = y
      self.dist = dist